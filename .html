<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
   <style>

   </style>
</head>
<body>
    <script>
        let arr = [1,2,3,4]
        // 累计器 当前值 当前索引 源数组
        let sum = arr.reduce((acc,cur,idx,src)=>{
            return acc*cur
        },1)
        console.log(sum)

        let arr1=[1,1,2,2,3,4,5,6,7,8,5,5,6]

        let sum1 = arr1.reduce((acc,cur)=>{
            if(!acc[cur]){
                acc[cur] = 1
            }else {
                acc[cur]++
            } 
            return acc
        },{})
        console.log(sum1)


        let pcd=[{
            id:1,
            name:'1',
            child:[{
                id:2,
                name:'2',
                child:[{
                    id:3,
                    name:'3'
                }]
            }]
        }]

        let idArr=[1,2,3]
        let res = pcd.reduce((acc,cur,idx,src)=>{
            console.log(src)
            console.log(cur)
            if(cur.id === idArr[0]){
                acc.push(idx)
                acc.push(src[idx].child.findIndex(bitem=>bitem.id===idArr[1]))
                acc.push(src[idx].child.find(bitem=>bitem.id===idArr[1]).child.findIndex(citem=>citem.id===idArr[2])) 
            }
            return acc
        },[])

        console.log(res)
    </script>
</body>
</html>